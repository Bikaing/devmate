# ===== LangChain / LangGraph 核心 =====
langchain==1.2.10
langchain-core==1.2.16
langchain-text-splitters==1.1.1
langgraph==1.0.9
langgraph-prebuilt==1.0.8
langgraph-checkpoint-postgres==2.0.8

# ===== 模型集成 =====
langchain-openai==1.1.10

# ===== 本地模型（进程内调用，无需独立服务）=====
transformers==4.51.0                   # MiniLM-L6-v2 意图分类
sentence-transformers==3.3.1           # BGE-Reranker 精排
sentencepiece==0.2.1                   # BGE-Reranker tokenizer 依赖
torch==2.5.1                           # 上述模型的底层依赖
FlagEmbedding==1.3.5                   # BGE-M3 本地嵌入（dense + sparse）；1.4.x 按 transformers 新 API 传 dtype，与锁定的 4.51 不兼容

# ===== Web 框架 =====
fastapi==0.117.1
uvicorn[standard]==0.32.1
python-multipart==0.0.20               # 文件上传
sse-starlette==2.1.3                   # SSE 流式响应

# ===== 数据库 =====
asyncpg==0.30.0                        # PostgreSQL 异步驱动
sqlalchemy[asyncio]==2.0.36            # 异步 SQL 工具
pymilvus==2.4.9                        # Milvus 向量库客户端（与服务器 v2.4.0 匹配）
setuptools==80.9.0                     # pymilvus 2.4.9 依赖 pkg_resources；setuptools>=82 已移除该模块，必须锁版本
marshmallow==3.26.1                    # pymilvus 传递依赖 environs 9.5 不兼容 marshmallow 4（缺 __version_info__）
numpy==1.26.4

# ===== 文档解析 =====
python-docx==1.1.2                     # Word 文档解析
pymupdf==1.24.14                       # PDF 解析

# ===== 数据校验 / 配置 =====
pydantic==2.11.4
pydantic-settings==2.11.0              # 读取 .env.local

# ===== 工具库 =====
httpx==0.28.1                          # 异步 HTTP 客户端
python-jose[cryptography]==3.3.0       # JWT 令牌
passlib[bcrypt]==1.7.4                 # 密码 bcrypt 哈希
bcrypt==4.0.1                          # passlib 1.7.4 不兼容 bcrypt>=5，锁定 4.x
python-dotenv==1.0.1                   # 加载 .env.local

# ===== MCP 协议 =====
mcp==1.26.0                            # 官方 MCP Python SDK（含 FastMCP）

# ===== 测试 =====
pytest==8.3.4
pytest-asyncio==0.24.0
